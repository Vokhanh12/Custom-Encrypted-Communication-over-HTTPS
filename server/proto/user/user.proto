syntax = "proto3";

package user;

option go_package = "server/api/user;user";

service UserService {
  rpc Login(LoginRequest) returns (LoginResponse);
}

message LoginRequest {
  string email = 1;
  string password = 2;
}

message LoginResponse {
  string token = 1;
}