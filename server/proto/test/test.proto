syntax = "proto3";

package test;

option go_package = "server/api/test;test";

service TestService {
  rpc Test(TestRequest) returns (TestResponse);
}

message TestRequest {
  string test = 1;
}

message TestResponse {
}